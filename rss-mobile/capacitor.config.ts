import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xiongsircool.aurora.mobile',
  appName: 'Aurora RSS Reader',
  webDir: 'dist'
};

export default config;

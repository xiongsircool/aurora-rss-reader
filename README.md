# RSS READER - 本地 RSS 阅读器 + AI 摘要

> 一个现代化的本地 RSS 阅读器，支持 AI 摘要、翻译和智能管理功能

## ✨ 功能特性

### 核心功能
- 📰 **RSS 订阅管理** - 添加、编辑、删除订阅源，支持分组管理
- 📖 **优雅阅读体验** - 三栏布局，自动提取全文内容
- 🔍 **搜索与过滤** - 全文搜索，支持未读/收藏筛选
- ⭐ **收藏与标记** - 标记已读、收藏喜欢的文章
- 📥 **OPML 导入导出** - 轻松备份和迁移订阅

### AI 增强功能
- 🤖 **AI 摘要** - 使用 GLM-4-Flash 自动生成文章摘要
- 🌐 **智能翻译** - 支持多语言翻译（中文、英文、日文、韩文等）
- 💾 **本地缓存** - 摘要和翻译结果本地缓存，节省 API 调用

### 用户体验
- 🎨 **深色/浅色主题** - 自动保存主题偏好
- ⚡ **快速响应** - 本地存储，离线可读
- 🔄 **自动刷新** - 定时获取最新文章
- 📱 **响应式设计** - 适配不同屏幕尺寸

## 🚀 快速开始

### 环境要求

- **Node.js**: >= 16.x
- **Python**: >= 3.9
- **pnpm**: >= 8.x（推荐）

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd RSSpage
```

#### 2. 安装后端依赖

```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -e .
```

#### 3. 配置环境变量

复制并编辑后端配置：

```bash
cp .env.example .env
```

编辑 `backend/.env`，配置 GLM API Key：

```env
GLM_API_KEY=your_api_key_here
```

#### 4. 初始化数据库

```bash
python -m scripts.migrate
```

#### 5. 安装前端依赖

```bash
cd ../rss-desktop
pnpm install
```

#### 6. 启动应用

```bash
# 并行启动前后端
pnpm dev
```

应用将在以下地址运行：
- 前端: http://localhost:5173
- 后端 API: http://localhost:8787

## 📖 使用指南

### 添加订阅

1. 在左侧边栏输入 RSS 源地址
2. 点击"添加订阅"按钮
3. 等待自动获取文章

### 管理订阅

- **编辑分组**: 悬停在订阅上，点击编辑图标 (✎)
- **删除订阅**: 点击删除图标 (🗑)
- **刷新订阅**: 在时间线头部点击"刷新订阅"

### OPML 导入导出

#### 导出订阅
1. 点击左侧边栏"导出 OPML"按钮
2. 保存 `.opml` 文件

#### 导入订阅
1. 点击"导入 OPML"按钮
2. 选择 `.opml` 或 `.xml` 文件
3. 查看导入结果统计

### 使用 AI 功能

#### 生成摘要
1. 打开任意文章
2. 在右侧详情面板找到"AI 摘要"卡片
3. 点击"生成摘要"按钮
4. 等待生成完成（自动缓存）

#### 翻译文章
1. 打开文章详情
2. 选择目标语言（中文/英文/日文/韩文）
3. 点击"翻译"按钮
4. 再次点击可切换回原文

### 搜索与过滤

- **搜索**: 在时间线上方输入关键词
- **过滤**: 点击"全部"/"未读"/"收藏"按钮
- **标记已读**: 自动标记，或手动点击文章
- **收藏文章**: 点击详情页的"收藏"按钮

## 🔧 开发指南

### 项目结构

```
RSSpage/
├── backend/              # Python FastAPI 后端
│   ├── app/
│   │   ├── api/routes/  # API 路由
│   │   ├── db/          # 数据库模型
│   │   ├── services/    # 业务逻辑
│   │   └── schemas/     # Pydantic 模型
│   ├── scripts/         # 脚本工具
│   └── .env             # 环境配置
├── rss-desktop/         # Vue + Electron 前端
│   ├── src/
│   │   ├── api/         # API 客户端
│   │   ├── stores/      # Pinia 状态管理
│   │   ├── views/       # 页面组件
│   │   └── types.ts     # TypeScript 类型
│   └── electron/        # Electron 主进程
└── README.md
```

### 可用脚本

#### 前端

```bash
pnpm dev              # 并行启动前后端
pnpm dev:frontend     # 仅启动前端
pnpm dev:backend      # 仅启动后端
pnpm build            # 构建生产版本
pnpm typecheck        # 类型检查
pnpm migrate          # 运行数据库迁移
```

#### 后端

```bash
python -m scripts.serve    # 启动开发服务器
python -m scripts.migrate  # 初始化数据库
```

### API 端点

#### 订阅管理
- `GET /api/feeds` - 获取订阅列表
- `POST /api/feeds` - 添加订阅
- `PATCH /api/feeds/{id}` - 更新订阅
- `DELETE /api/feeds/{id}` - 删除订阅
- `POST /api/feeds/{id}/refresh` - 刷新订阅

#### 文章管理
- `GET /api/entries` - 获取文章列表
- `PATCH /api/entries/{id}` - 更新文章状态

#### AI 功能
- `POST /api/ai/summary` - 生成摘要
- `POST /api/ai/translate` - 翻译文章

#### OPML
- `GET /api/opml/export` - 导出 OPML
- `POST /api/opml/import` - 导入 OPML

## 🎨 主题定制

主题使用 CSS 变量实现，可以在 `AppHome.vue` 中修改：

```css
:root {
  --bg-base: #f5f7fb;
  --bg-surface: #ffffff;
  --text-primary: #0f1419;
  --text-secondary: #6c7384;
  --border-color: rgba(15, 17, 21, 0.08);
  --accent: #ff7a18;
}

:root.dark {
  --bg-base: #0f1115;
  --bg-surface: #181b22;
  --text-primary: #f5f6fa;
  --text-secondary: #9ba1b3;
  --border-color: rgba(255, 255, 255, 0.1);
  --accent: #ff8a3d;
}
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

- [FastAPI](https://fastapi.tiangolo.com/) - 现代化的 Python Web 框架
- [Vue 3](https://vuejs.org/) - 渐进式 JavaScript 框架
- [Electron](https://www.electronjs.org/) - 跨平台桌面应用框架
- [GLM-4](https://open.bigmodel.cn/) - 智能 AI 模型
- [feedparser](https://feedparser.readthedocs.io/) - RSS 解析库

## 📞 联系方式

如有问题或建议，请提交 Issue 或联系开发团队。

---

**祝你阅读愉快！** 📚✨
